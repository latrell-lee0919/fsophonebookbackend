{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../phonebook-frontend/ && npm run build --prod && cp -r build ../phonebook-backend",
    "deploy": "git push heroku main:main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/latrell-lee0919/fsophonebookbackend.git"
  },
  "author": "Latrell Lee",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/latrell-lee0919/fsophonebookbackend/issues"
  },
  "homepage": "https://github.com/latrell-lee0919/fsophonebookbackend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
